version: '3.8'

services:
  frontend:
    build:
      context: .
    container_name: conquertactoe_frontend
    ports:
      - "${PORT1}:${PORT2}"
    networks:
      - conquer-network

networks:
  conquer-network:
    external: true
